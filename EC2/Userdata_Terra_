sudo yum install -y yum-utils
sudo yum-config-manager --add-repo https://rpm.releases.hashicorp.com/AmazonLinux/hashicorp.repo
sudo yum -y install terraform

#(remember you still need to configure aws cli + aws configure for IAM access for manual testing.)#

# git clone https://github.com/JenniaGo/Terraform_projects.git
# For Ubuntu instance version:
#sudo apt-get update && sudo apt-get install -y gnupg software-properties-common curl
#curl -fsSL https://apt.releases.hashicorp.com/gpg | sudo apt-key add -
#sudo apt-add-repository "deb [arch=amd64] https://apt.releases.hashicorp.com $(lsb_release -cs) main" -y
#sudo apt-get update && sudo apt-get install terraform -y
#terraform -help # (test if terraform installtion was successfull)


#sudo apt install awscli
#aws configure
#nano variables.tf # (add the template credtianals)
#terraform init # (start the bucket s3 creation)
#terraform plan
#terraform apply
#terraform destroy
